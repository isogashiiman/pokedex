<!DOCTYPE html>
<html>
  <head>
    <title>Pokedex</title>
    <meta charset="utf-8">
    <meta http-equiv=X-UA-Compatible content="IE=edge">
    <meta name="viewport-width", initial-scale=1>
    <title>Pokedex</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <header>
  	<p>
  		<%= link_to "Liste des Pokemons", pokemons_path,
  		class: 'btn-default' %>
  		<%= link_to "Liste des Types", types_path,
  		class: 'btn-default' %>
  		<%= link_to "Liste des Attaques", moves_path,
  		class: 'btn-default' %>
  	</p>
  </header>

  		<% flash.each do |name, msg| %>
  		<div class="alert alert.class(name) %>">
  			<a class="close" data-dismiss="alert">X</a>
  			<%= content_tag :div, msg, id:"flash_#{name}" %>
  		</div>

	  <div class="container">
	    <%= yield %>
	   </div>
  </body>
</html>
